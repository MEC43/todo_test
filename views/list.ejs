<%- include('header.ejs') %>
    <h2>리스트 목록 보기</h2>
    <ul>
        <%- posts.length==0 ? `<li>등록된 리스트가 없음</li>` :
            posts.map(post => `
            <li>
                <p>${post._id}</p>
                <p>${post.title}</p>
                <p>${post.dateOfGoal}</p>
                <!-- <button>삭제</button> -->
                <a href="/detail/${post._id}">상세페이지</a>
            </li>
            `).join('') %>
    </ul>
    <%- include('footer.ejs') %>